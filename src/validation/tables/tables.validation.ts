import { users } from './users';
import { workspaceUsers } from './workspace-users';
import { workspaces } from './workspaces';

export const tables = () => ({
  users,
  workspaceUsers,
  workspaces,
});
